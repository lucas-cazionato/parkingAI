# Configurações Aplicação
spring.application.name=ms-usuario
server.port=3001

# Config para rodar local 
# Configurações MongoDB
#  spring.data.mongodb.host=localhost
#  spring.data.mongodb.port=27017
#  spring.data.mongodb.authentication-database=${MONGO_AUTH_DB}
#  spring.data.mongodb.database=${MONGO_DB}

# Config para rodar no docker 
spring.data.mongodb.host=mongo-db
spring.data.mongodb.port=27017
spring.data.mongodb.authentication-database=admin
spring.data.mongodb.database=${MONGO_INITDB_DATABASE}


# E-mail
spring.mail.host=sandbox.smtp.mailtrap.io
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
#Alteração para rodar no Windows
spring.mail.properties.mail.smtp.ssl.trust=* 